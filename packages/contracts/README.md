# `@voice-input-layer/contracts`

Shared TypeScript domain contracts for the voice session, transcript, cleanup result, text target, and usage event models.

Keep this package dependency-light. Platform implementations should depend on these contracts, not on each other's native code.
