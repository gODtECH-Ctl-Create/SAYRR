{
  "name": "voice-input-layer",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "pnpm --recursive exec tsc --noEmit"
  }
}
